# M166 严格性：迭代期间向**被迭代 list** 追加 ⇒ R1003（三轨同码同文）
# 修前：解释/C 轨按活长度多迭代（打印 1,2,3,9,…）vs VM 轨按快照（1,2,3）⇒ 静默分歧。
def main():
    var l = [1, 2, 3]
    for x in l:
        print(x)
        l.append(9)
    print("never")
