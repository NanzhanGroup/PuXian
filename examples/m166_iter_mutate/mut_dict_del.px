# M166 严格性：迭代期间从**被迭代 dict** 删键 ⇒ R1003（三轨同码同文）
# 修前：VM 轨越界 rc=1 vs 解释轨键快照（仍迭代到已删的键）vs C 轨中途停 ⇒ 三轨分叉。
def main():
    var d = {"a": 1, "b": 2, "c": 3}
    for k in d:
        print(k)
        d.remove(k)
    print("never")
