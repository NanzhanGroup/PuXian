# ============================================================
# M116（qg-issue 71 D2）负控：字典**缺键下标**必须在两条轨上都报 R1008
# ------------------------------------------------------------
# 修复前：编译轨（C/VM）静默返回 null，解释轨抛 R1008 —— 同一份源码两轨行为不同
#   （违背 docs/MINI_SUBSET §五「双模式行为一致」与 docs/M65_PLAN.md 的 R1008 口径）。
#   实测被它坑过：ws-center 移植时 `str(r["role"])`（键缺失）在编译轨下得到字符串
#   "null" 并被**写进数据库**，解释轨则当场报错 —— 静默数据损坏。
# 判据（verify.sh）：两条轨都必须 **非零退出**，且 stderr 含 R1008 与键名；
#   且出错点之后的 print("after") **不得**出现（不是"打一行日志继续"）。
# ============================================================
let d = json_parse("{\"a\": 1}")
print("before")
print(d["nope"])
print("after")
