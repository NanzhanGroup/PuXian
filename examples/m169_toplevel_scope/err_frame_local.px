# M169 严格性用例：帧内「赋值式」绑定未声明名 ⇒ **帧局部**，模块体读不到
#   解释轨（参考）：f() 内部 `zz = 5` 建帧局部 ⇒ 模块体 `print(zz)` 报「未定义变量」
#   修前 VM 轨因 `bc_is_global` 惰性登记（`def g(): return zz` 的读把它变成「全局」）
#   ⇒ 结论随发射顺序变化；本用例把顺序固定为「先写后读」以暴露该差异。
def f():
    zz = 5
    return zz
print("f=" + str(f()))
print("zz=" + str(zz))
