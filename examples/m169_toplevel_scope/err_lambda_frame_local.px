# M169 严格性用例（闭包版）：闭包体内「赋值式」绑定未声明名 ⇒ **闭包帧局部**
#   解释轨（参考）：f() 内部 `xx = 5` 建**帧**局部 ⇒ 模块体的 `g()` 读不到 ⇒「未定义变量」
#   修前 VM 轨因 `bc_is_global` 惰性登记（`def g()` 先发射、把 xx 登记成全局）⇒
#   闭包里的赋值被当成**全局写**（r2=5，与解释轨分叉）；C 轨同病（闭包帧缺 hoist/装箱）。
let f = fn ():
    xx = 5
    return xx
print("r1=" + str(f()))
def g():
    return xx
print("r2=" + str(g()))
