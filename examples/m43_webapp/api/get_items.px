# GET /api/items —— 子目录前缀路由（api/ 目录 → /api 前缀，fnname api_get_items）
def api_get_items(req, params):
    return {"status": 200, "body": json_stringify({"items": [{"id": 1, "name": "甲"}, {"id": 2, "name": "乙"}]}), "headers": {"Content-Type": "application/json"}}
