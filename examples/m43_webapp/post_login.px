# POST /login —— 限流演示（routegen 读此注释 → route 第 4 参 opts → 10 次/60 秒/每 IP）
# rate_limit: 10/60
def post_login(req, params):
    return {"status": 200, "body": json_stringify({"login": true, "body": req["body"]}), "headers": {"Content-Type": "application/json"}}
