# ============================================================
# hello_a64.px —— M67 阶段一：多架构交叉编译最小验证（四档矩阵用例 1/4）
# ------------------------------------------------------------
# 验证闭环：.px → bootstrap/pxc 生成 C → aarch64-linux-musl-gcc 静态交叉编译
#           → file 确认 ELF ARM aarch64 → qemu-aarch64 运行断言
# 构建（需 aarch64-linux-musl-gcc 在 PATH + 预置 aarch64 交叉库）：
#   tools/pxc build --no-quic --cc aarch64-linux-musl-gcc \
#     --mbedtls-lib runtime/mbedtls/lib-aarch64 \
#     --sqlite-obj runtime/third_party/sqlite3/sqlite3-aarch64.o \
#     examples/m67_aarch64/hello_a64.px
# 运行：./examples/m67_aarch64/build/hello_a64（x86 pxi 直跑同输出，双模式一致）
# ============================================================
def main():
    print("hello from multiarch (m67)")
    print("PX_ARCH_CROSS_OK")
