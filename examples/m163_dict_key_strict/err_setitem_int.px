# M163 门 ④：**索引赋值** `d[k] = v`（k 非字符串）—— 三轨同码同文（缺陷 171）
# 文档口径（docs/DICT_STRICT_MIGRATION.md §1）：`d[i]` 键不是字符串 ⇒
#   R1002 `字典索引键必须是字符串`。修前解释轨独用「字典键必须是字符串」（同码不同文）。
def main():
    var d = {"a": 1}
    d[2] = 3
    print("len=" + str(len(d)))

