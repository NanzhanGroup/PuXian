# M163 门 ③-e：**字典推导式**非字符串键 ⇒ R1002「字典键必须是字符串，实际是 int」
# 修前：C 轨静默丢弃（得 `len=0`）；VM 轨走 `set` 方法报「方法 set 参数 1 需要 string」
#   （同码 R1002 但**词条**与解释轨不一致）。
def main():
    let d = {x: x + 10 for x in [1, 2, 3]}
    print("len=" + str(len(d)))

