# M163 门 ③-b：字面量**混合键**（字符串在前、非字符串在后）⇒ 仍须报 R1002（逐项检查）
# 修前：VM / C 轨只丢非字符串那项（`{"a": 1, "b": 2, 3: 4}` 得 `len=2`）—— 静默丢项最难查。
def main():
    let d = {"a": 1, "b": 2, 3: 4}
    print("len=" + str(len(d)))

