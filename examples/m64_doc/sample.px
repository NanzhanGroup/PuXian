# ============================================================
# M64-S5 doc 自测语料：## 文档注释（紧跟顶层定义）
# ------------------------------------------------------------
# 语义验证点（对齐 Rust doc.rs）：
#   1) 文件头 ## 说明块并入首个定义文档（Rust 行结构不打断待定注释）
#   2) 普通 # 注释打断待定文档 → greet 显示"无文档注释"
#   3) struct/enum 归入 "## 类型" 节
# ============================================================
## 普贤自测样本
## 本文件用于 pxdoc 生成器验证（文件头说明并入首个定义）

## 计算两数之和
## 参数 a、b：加数，返回和
def add(a: int, b: int) -> int:
    return a + b

# 普通注释打断待定文档：greet 应显示"无文档注释"
def greet():
    return "hi"

## 平面点类型
struct Point:
    x: float
    y: float

enum Color:
    Red
    Green
    Blue
