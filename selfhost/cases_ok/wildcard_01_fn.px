# M113-S0（Issue 57）正例：函数内弃元赋值 `_ = f()`
# 背景：默认轨（VM / `tools/px`）曾把 `_` 当不可变变量 → 任何 `_ = f()` 判 E3002（致命）。
# 注：main 由编译器隐式调用（顶层再写一次 `main()` 会执行两遍 —— 两轨同行为，见报告）。
def compute():
    return 42
def main():
    _ = compute()
    print("fn-ok")
