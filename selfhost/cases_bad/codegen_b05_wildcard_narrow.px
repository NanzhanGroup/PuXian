# M113-S0（Issue 57）反例：豁免必须**只覆盖 `_`**
#   `let _ = 1` + `_ = 2` 合法（不报）⇒ 首行错误必须是 `y`，
#   若豁免越界（漏报 y）或未生效（先报 _）本用例都会失败。
def main():
    let _ = 1
    _ = 2
    let y = 3
    y = 4
    print("never")
main()
